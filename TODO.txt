------------------------------------------Кадуцей------------------------------------------Протокол встречи от 08.09.2021.ВСМ. Доработка существующего функционала:---------1. Закрепить горизонтальный бегунок в блоке «Таски». Количество строк 8---------2. Доработать ластик или что-нибудь вместо него.---------3. Блоки, если их закрыть и снова открыть, отображаются по 2-3-4 раза. Зависит от количества переоткрытий блока.---------4. Убрать время в поле «Дата» в шапке. Оставить только дату (гггг.мм.дд).---------5. Не ищет по номеру ТТН, если в нем есть буквы.ВСМ. Новый функционал:-------------------------------1. Смена статуса документа / деталей.    Сделано   -- серверная обработка нового статуса   function changeStateBacchus(oldS,cadbuf,cadsap,inout){   var newSt = document.getElementById("listOfStatusesCadu").value;   var row_status = document.getElementById("rowStatusCh");   if(inout==1){   	let xhrCh = new XMLHttpRequest();   	xhrCh.onreadystatechange = function() {   		if (xhrCh.readyState !== 4) return;   		if (xhrCh.status == 200) {   			var ok_or_no = xhrCh.responseText;   			if(ok_or_no=="ok")   				alert("buf changed!");   			//MagOut();   		}   	}   	var body = 'cadbuf='+cadbuf.replaceAll(/\s/g,"")+   	'&cadsap='+cadsap.replaceAll(/\s/g,"")+   	'&cadstatus'+newSt+   	'&cadparam=inCaduChange';   	xhrCh.open('POST', '/test/cadusearch', true);   	xhrCh.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded')   	xhrCh.send(body);   }   else{   	let xhrOut = new XMLHttpRequest();   	xhrOut.onreadystatechange = function() {   		if (xhrOut.readyState !== 4) return;   		if (xhrOut.status == 200) {   			var ok_or_no = xhrOut.responseText;   			if(ok_or_no=="ok")   				alert("buf changed!");   			MagOut();   		}   	}   	var body = 'cadbuf='+cadbuf.replaceAll(/\s/g,"")+   	'&cadsap='+cadsap.replaceAll(/\s/g,"")+   	'&cadparam=outCaduChange';   	xhrOut.open('POST', '/test/cadusearch', true);   	xhrOut.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded')   	xhrOut.send(body);   }   }2. Возможность перезапуска документа из ВСМ.3. Возможность перезапуска потока 10, 13 из БД РЦ.Вопрос на «подумать»:Вход в Меркурий ХС, выбор ХС и завода. Возможно ли, чтобы ВСМ по запросу возвращал ссылку на «домашнюю страницу завода» в Меркурии? Сделать кнопку.Дополнительно:Некорректный номер ТТН в потоке V44 из WMOS. Сделать робота. Разработать алгоритм автоматизации исправления ошибки.